# ── Secrets ────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── Python ─────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── OS / IDE ───────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# ── Project: temp screenshots & playwright artifacts ──────
.playwright-mcp/
/v*_full.png
/tmp/
*.log

# ── Project: third-party vendored repos (own .git history) ─
social-auto-upload/
.claude/skills/news-aggregator-skill/

# ── Dead symlinks pointing into .agents/ (which itself is ignored) ─
.claude/skills/gstack
skills/gstack
skills/

# ── Project: agent-only sub-dirs we don't intend to publish ─
.agents/

# ── Render artifacts that should be regenerated, not stored ─
# (we KEEP brand_assets/* and daily_reports/* — they're inputs/outputs we want versioned)
scripts/wechat_render/__pycache__/

# ── 钉钉发送 payload 留底（本地排查用，不入 git）─
send-logs/

# ── Claude Code 本地 cron 任务（含 webhook secret，不入 git）─
.claude/scheduled_tasks.json
.claude/scheduled_tasks.lock
